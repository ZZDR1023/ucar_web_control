# 05月19日 17:50 - 人体跟随链路排查总结

## 1. 已解决的底层问题 (打通了链路)
* **硬件抢占死锁解决**：此前 Web 相机桥无法启动，原因是 `/dev/video0` 被旧进程占用。已在 `server.py` 中加入了硬核强杀逻辑 (`fuser -k /dev/video0`)，目前相机桥可以 100% 成功接管摄像头。
* **话题超时判定修复**：Web 后端之前只等 5 秒且检测逻辑有问题，导致误判。目前已将超时时间拉长至 10 秒，并使用 `rospy.wait_for_message`，成功解决了由于硬件预热导致的“未就绪”误报。
* **原厂 `ori_im` 报错与分辨率修正**：之前原厂 `person_follow.py` 启动后无反应并在日志疯狂报错 `ori_im 未赋值`。根因已查明：原厂代码硬编码要求 `1280x720` 的图像输入并进行 reshape，而我们的相机桥发的是 `640x480`。**目前已将 `server.py` 中相机桥的启动参数修改为 `_width:=1280 _height:=720`，该底层冲突已解决。**

## 2. 当前系统状态
* 小车 IP: `10.68.225.179`
* `ucar_web.service` 和 `ucar_sensors.service` 均正常 Active。
* `ucar_camera_bridge.py` 正常运行，并以 1280x720 稳定发布 `/ucar_camera/image_raw`。
* `person_follow` 的 ROS 节点和服务已经存在于系统中。

## 3. 当前遗留问题 (Next Steps)
在底层分辨率和冲突修复后，点击 Web 端的“开始跟随”，依然显示：**“运行中：人体跟随未就绪”**，且小车面对人体无动作。
需要排查：
1. 原厂 `person_follow.py` 的图像回调是否还有其他报错（需查阅最新的 `/tmp/person_follow.log`）。
2. Web 后端在调用 `/person_follow/start_person_detect` 等服务后，前端的状态缓存是否没有正确更新，导致 UI 一直卡在“未就绪”的提示上。
3. 原厂检测节点是否真正识别到了目标（需要查 `get_detect_info` 的返回值）。
